import {TrendingLabel} from "../model/TrendingItem.ets"

@Entry
@Component
export struct TrendingRow {
  private id: number;
  private title: string;
  private reading: string;
  private label: TrendingLabel;
  build() {

  }
}